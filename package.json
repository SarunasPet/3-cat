{
  "name": "3-cat",
  "version": "1.0.0",
  "description": "Cat",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --port=3333 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "html"
  ],
  "author": "SarunasPet",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.9"
  }
}
